# Cloudflare Worker (cloudflare/) — local artifacts and secrets.
node_modules/
dist/
.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example
worker-configuration.d.ts
*.log
coverage/
# Local OpenBao binary used for the custody round-trip tests (NOT committed —
# it's a large vendored binary; the test harness downloads/launches it locally).
.bao-bin/
.bao-test/
