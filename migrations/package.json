{
  "name": "pinning-service-migrations",
  "version": "1.0.0",
  "description": "Database migration scripts for pinning service",
  "main": "migrate-sqlite-to-postgres.ts",
  "scripts": {
    "migrate": "npx ts-node migrate-sqlite-to-postgres.ts",
    "migrate:help": "echo 'Usage: npm run migrate (with POSTGRES_* and optionally SQLITE_PATH env vars set)'"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.8",
    "@types/node": "^20.10.0",
    "@types/pg": "^8.10.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.2"
  }
}
